# name: CI for RSpec Tests

# on:
#   push:
#     branches:
#       - main
#   pull_request:
#     branches:
#       - main

# jobs:
#   test:
#     runs-on: ubuntu-latest

#     services:
#       postgres:
#         image: postgres:13.5 # PostgreSQLのマイナーバージョンを固定
#         env:
#           POSTGRES_USER: postgres
#           POSTGRES_PASSWORD: password
#           POSTGRES_DB: myapp_test
#         ports:
#           - 5432:5432
#         options: >-
#           --health-cmd pg_isready
#           --health-interval 10s
#           --health-timeout 5s
#           --health-retries 5

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v3

#       - name: Cache Gemfile dependencies
#         uses: actions/cache@v3
#         with:
#           path: vendor/bundle
#           key: ${{ runner.os }}-bundle-${{ hashFiles('**/Gemfile.lock') }}
#           restore-keys: |
#             ${{ runner.os }}-bundle-

#       - name: Set up Ruby
#         uses: ruby/setup-ruby@v1
#         with:
#           ruby-version: '3.1.4'
#           bundler-cache: true

#       - name: Prepare Database Configuration
#         run: |
#           cp config/database.yml.ci config/database.yml

#       - name: Install dependencies
#         run: |
#           gem install bundler
#           bundle install --jobs 4 --retry 3

#       - name: Set up database
#         env:
#           RAILS_ENV: test
#           DATABASE_URL: postgres://postgres:password@localhost:5432/myapp_test
#         run: |
#           bin/rails db:create
#           bin/rails db:migrate

#       - name: Run RSpec tests
#         env:
#           RAILS_ENV: test
#         run: |
#           bundle exec rspec

#       - name: Show Rails Logs (for debugging)
#         if: failure()
#         run: |
#           cat log/test.log || echo "No logs found"
